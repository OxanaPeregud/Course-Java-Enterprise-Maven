<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">

    <changeSet id="01" author="Oxana">
        <comment>Creating Tables</comment>
        <sql>
            create table people.address
            (
            address_id int auto_increment
            primary key,
            apartment int null,
            house int null,
            street varchar(255) null
            );

            create table people.person
            (
            person_id int auto_increment
            primary key,
            age int null,
            first_name varchar(255) null,
            last_name varchar(255) null
            );

            create table people.person_address
            (
            address_address_id int not null,
            person_person_id int not null,
            primary key (address_address_id, person_person_id),
            constraint FKq2jqgrsqkjjef7dg7xcopqnwl
            foreign key (person_person_id) references people.person (person_id),
            constraint FKr11uf0a3rj6u15itwmvvekvo0
            foreign key (address_address_id) references people.address (address_id)
            );
        </sql>
    </changeSet>
</databaseChangeLog>
